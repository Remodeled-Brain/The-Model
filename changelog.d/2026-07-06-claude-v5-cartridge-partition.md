- **[candidate]** v0.05 v5 partition: split `06` handle/terminology content into content-indifferent CATEGORIES (kept in core) and neuroscience INSTANCES (moved to new `model/cartridges/neuroscience.yaml`). Core `06` now carries category shapes only — `global_shift` and `terminology_guard` generalized, `handle_instances_source` pointer added; grep confirms zero neuroscience terms remain in the core. Cartridge is an OPTIONAL MODULE (not in `source_files`, not inlined into `10`): connected/capable models load it for sharp domain stripping, confined models strip by category only (valid, generic). Build footer names `domain-cartridge -> cartridges/neuroscience.yaml`. Fork test: retarget the core to another domain by swapping the cartridge, touching no core file. (claude, 2026-07-06)
