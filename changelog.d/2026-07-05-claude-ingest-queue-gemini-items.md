- **[candidate]** Executed the gated Gemini/ChatGPT ingest patch queue: split `model/manifest.json` into `source_files`/`generated` and added `scripts/build_master_prompt.py` (generates `10` per ADR 0001, recursion-guarded); added `gate_intersection_semantics` (notes accumulate, permissions take most-restrictive cap) and qualitative `visibility_priority_cap` to `05`; resynced `07` fixtures to consolidated rule names and added Fixture 11 (visibility-count exploit); added minimal `post_strip_claim_form` slot taxonomy to `06`/`03`; added candidate `model/record_schema_v1.yaml` (blocked on ADR 0003 identity). Numeric visibility scaling and the full causal slot decomposition were deferred, not adopted. All items candidate except the manifest split. (claude, 2026-07-05)
