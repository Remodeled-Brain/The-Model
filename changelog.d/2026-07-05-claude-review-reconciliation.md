- **[candidate]** Reconciled the ChatGPT (hold + defects) and Gemini (accept, partial read) reviews of the v0.05 v4 batch and applied the agreed fixes: synced `01`/`09` off retired `residue`/rule-name terminology; added `grouping_variable` and `not_applicable`/`unresolved` gate-result states to `record_schema_v1`; dropped the unimplementable `min_across_failed_gates` (kept notes-accumulate + permissions-intersect); marked `support_type_ladder`, `visibility_priority_cap`, `post_strip_claim_form` as `reserved_for_v0_07`; converted Phase-A handle conditions to `handle_flags` adjudicated in Phase B (`03`/`06`); added a non-reject guard to the controlled-sameness rule; tightened Fixture 11; added DOI path-safety, model-identity-vs-run-id separation, and supersession disambiguation to ADR 0003 (still CANDIDATE); clarified in CONTRIBUTING that `model/` holds both adopted and candidate material keyed by `status:`. Checkpoint 4 and consensus mechanisms remain open blockers. Fable did not review (safeguard bounce). (claude, 2026-07-05)
