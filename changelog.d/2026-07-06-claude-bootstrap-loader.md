- **[candidate]** Turned `10_single_file_master_prompt.txt` into a capability-gated bootstrap: `build_master_prompt.py` now wraps the concatenated core with an operator-procedure header (provenance-anchored, module-loader framing, ingest = support-layer only) and an OPTIONAL MODULES footer (live-spec = the repo as authoritative current version; distributed-ingest = `decisions/0003` for connected/capable models). Header/footer live in the build, not hand-edited onto the generated file. Depth of follow is capability-gated and emergent — a model confined to the file does bounded ingest and stops (reduced function, not failure); a connected/capable one follows the named references onward. Nothing stripped. (claude, 2026-07-06)
