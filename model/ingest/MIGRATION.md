# Migration note

The active ingest files are being consolidated under this directory. Version-specific packet prose and stale generated artifacts are removed once their active content has an explicit consumer here. Frozen packets and Git history remain the reproducibility record.
