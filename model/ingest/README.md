# Active ingest subsystem

The files in this directory define adaptive evidence maintenance for The Model. The authoritative load graph is `model/manifests/ingest.json`.

The subsystem is support-layer only. It produces reviewed evidence records for the primary runtime's evidence binder. It does not compile source questions or determine the final answer shape.
