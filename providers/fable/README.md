# Fable (Claude Fable 5) provider notes

Adapters, overrides, known incompatibilities, and notes specific to Claude Fable 5 only. Do **not** copy the canonical model here.

Fable's current role is primarily as a **stress-test reviewer** of candidate work. Any changes it makes go on a `fable/` branch, not `main`. See `/FABLE.md`.
